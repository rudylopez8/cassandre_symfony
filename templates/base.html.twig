<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Cassandre Communication{% endblock %}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
    <link rel="stylesheet" href="{{ asset('css/style.css') }}">
    {% block stylesheets %}{% endblock %}
</head>
<body>
    {% block navbar %}
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary sticky-top">
            <div class="container-fluid">
                <a class="navbar-brand" href="{{ path('index') }}">Cassandre</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.pathInfo == '/' %}active{% endif %}" href="{{ path('index') }}">Tableau de bord</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.pathInfo == '/audit' %}active{% endif %}" href="{{ path('audit') }}">Audits</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.pathInfo == '/certification' %}active{% endif %}" href="{{ path('certification') }}">Certifications</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.pathInfo == '/admin' %}active{% endif %}" href="{{ path('admin') }}">Administration</a>
                    </li>
                </ul>
                    <span class="navbar-text me-3">Bonjour, <strong>Auditeur</strong></span>
                    <a href="{{ path('login') }}" class="btn btn-outline-light">Déconnexion</a>
                </div>
            </div>
        </nav>
    {% endblock %}

    <main class="container py-4">
        {% block body %}{% endblock %}
    </main>

    {% block javascripts %}
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <script src="{{ asset('js/main.js') }}"></script>
    {% endblock %}
</body>
</html>
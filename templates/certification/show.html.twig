{% extends 'base.html.twig' %}

{% block title %}Certification{% endblock %}

{% block body %}
    <h1>Certification</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ certification.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ certification.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ certification.description }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ certification.price }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ certification.duration }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ certification.isActive ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ certification.createdAt ? certification.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ certification.updatedAt ? certification.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_certification_index') }}">back to list</a>

    <a href="{{ path('app_certification_edit', {'id': certification.id}) }}">edit</a>

    {{ include('certification/_delete_form.html.twig') }}
{% endblock %}
